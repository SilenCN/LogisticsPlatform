create database LogisticsPlatform;

create table test(id INTEGER primary key AUTO_INCREMENT,name TEXT NOT NULL,password TEXT NOT NULL);

create table user(id INTEGER primary key AUTO_INCREMENT,name TEXT NOT NULL,password TEXT NOT NULL,type INTEGER NOT NULL,info BOOLEAN,createTime BIGINT);

create table orders(
    id INTEGER primary key AUTO_INCREMENT,
    carId INTEGER NOT NULL,
    ownerId INTEGER NOT NULL,
    status INTEGER,
    createTime BIGINT);

